# 项目结构说明

本文档简述 `vlmpddl` 项目中三个核心文件夹的内容和作用。

---

## 1. `yolo_project` — 目标检测

**作用**：基于 YOLOv8 的部件目标检测，用于识别场景中的 regulator、battery、pump 等部件（含颜色细分：green/red/blue）。

**主要内容**：
- `config/data.yaml`：数据集配置（9 类：green/red/blue_regulator、green/red/blue_battery、green/red/blue_pump）
- `data/images/`：原始图片
- `data/labels/`：YOLO 格式标注
- `data/info/`：原始 txt 信息
- `main.py`：统一脚本（训练、推理）
- `evaluate_pddl_recognize.py`：针对 PDDL/VLM 测试集的评估脚本

**典型用法**：训练 YOLOv8 模型，对图片做目标检测，并将检测结果用于 VLM 的 YOLO 提示注入（`sft_yolo_prompt` 评测模式）。

---

## 2. `vlm` — 视觉语言模型训练与评测

**作用**：对 Qwen2.5-VL 进行 LoRA SFT 训练，实现「图像 → PDDL」的视觉理解与任务规划。

**主要内容**：
- `train/train_sft.py`：训练与评测主脚本
- `train/ds_z2_offload_config.json`：DeepSpeed ZeRO-2 + CPU offload 配置
- `train/output_lora_refactor/`：训练输出（checkpoint、final_model、评测结果）
- `models/`：模型目录（如 Qwen2.5-VL-3B-Instruct）

**典型用法**：
- 训练：`deepspeed vlm/train/train_sft.py --mode train`
- 评测：base / sft / sft_yolo_prompt 三种模式
- 对比评测：`--eval_compare_all` 生成 `eval_compare_init.json` 与 `eval_compare_init.md`

---

## 3. `pddl_data` — PDDL 数据集与训练数据

**作用**：存放「图像 → PDDL」的视觉规划数据集，包含训练/测试 JSON、指令、图片等。

**主要内容**：
- `data_vl_train_130.json` / `data_vl_test_50.json`：VLM 训练/测试集（对话格式）
- `real_pictures/`：真实场景图片
- `instructions/`：自然语言指令（`.nl`）
- `generate_image2pddl_dataset.py`：从 PDDL 提取 goal 并生成训练数据集
- `split_data_vl_train_test.py`：划分训练/测试集

**数据格式**：每条样本为「图片 + PDDL 域定义 + 目标描述」→ 模型输出 `(:objects ...) (:init ...) (:goal ...)` 三段 PDDL。

**典型用法**：作为 VLM 训练与评测的输入数据源；`train_sft.py` 默认使用 `data_vl_train_130.json`。

---

## 三者关系

```
pddl_data (数据)  →  vlm (训练/评测)  ←  yolo_project (目标检测辅助)
     │                      │                        │
     │                      │                        └─ YOLO 检测结果可注入 prompt
     │                      └─ 使用 data_vl_train_130.json 等
     └─ 生成 data_vl_train.json / data_vl_test.json
```

整体流程：`pddl_data` 提供图像与 PDDL 标注；`vlm` 负责训练 VLM 并评测；`yolo_project` 提供目标检测能力，可增强 VLM 的 prompt 输入。
